supertonic>=1.3.0
numpy>=1.26
soundfile>=0.12
librosa>=0.10
fastapi>=0.100
uvicorn[standard]>=0.27
python-multipart>=0.0.7

# ECAPA-TDNN speaker encoder (speechbrain/spkrec-ecapa-voxceleb, Apache 2.0)
speechbrain>=1.0
torch>=2.1
torchaudio>=2.1

# ONNX runtime: pick ONE flavor per machine.
#   Desktop (RTX 4060):   pip install onnxruntime-gpu     (CUDA)
#   Laptop (Ryzen AI 7):  pip install onnxruntime-directml (DirectML)
# The engine auto-selects CUDA > DirectML > CPU from whatever is installed.
onnxruntime-directml ; platform_system == "Windows"
